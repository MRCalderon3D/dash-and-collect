namespace DashAndCollect
{
    /// <summary>
    /// Identifies which modifier chain was completed (TDD §5).
    /// None = no active modifier.
    /// </summary>
    public enum ModifierType { None, Dash, Shield, Surge }
}
